# n-gram-model
This project implements an N-gram Language Model built from scratch. It allows the user to tokenize a textual corpus, generate N-grams and calculate their probabilities, and predict a the next word given a context to generate sentences based on the trained model. 
